"use client";
import React from "react";
import LoginForm from "../components/LoginForm";

const page = () => {
  return (
    <main className="flex h-full w-full p-4 flex-col justify-center items-center overflow-x-hidden">
      <LoginForm />
    </main>
  );
};

export default page;
